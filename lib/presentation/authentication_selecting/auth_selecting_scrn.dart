import 'package:flutter/material.dart';

class AuthSelectingScreen extends StatelessWidget {
  const AuthSelectingScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
